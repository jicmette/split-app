import { Text, View } from 'react-native';

export default function Modal() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontSize: 20 }}>This is the Add Expense Modal</Text>
    </View>
  );
}